{
  "buildCommand": "cd ../shared-lib && npm install && cd ../container-app && npm run build",
  "outputDirectory": "dist/container-app",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
